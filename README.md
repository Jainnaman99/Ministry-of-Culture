
  # Build According to Prompt

  This is a code bundle for Build According to Prompt. The original project is available at https://www.figma.com/design/H58U47zIi2ys04WtHguaih/Build-According-to-Prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  